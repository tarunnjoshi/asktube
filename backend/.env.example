# Copy this file to .env and paste your real keys in.
#   cp .env.example .env
# .env is gitignored - it must NEVER be committed.

# Google AI Studio -> https://aistudio.google.com/api-keys
GOOGLE_API_KEY=paste_your_gemini_key_here

# Optional: only needed to silence a HuggingFace download warning.
HF_TOKEN=
